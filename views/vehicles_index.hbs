<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{bodyType}} Cars - Merryfield Trading Ltd</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
    
    <!-- Bootstrap and icons -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
          integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh"
          crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/86198f839e.js" crossorigin="anonymous"></script>
    <link rel="stylesheet"
          href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Kaushan+Script" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Droid+Serif:400,700,400italic,700italic" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Roboto+Slab:400,100,300,700" rel="stylesheet">
    
    <!-- Custom styles -->
    <link rel="icon" href="/images/auto_favi.ico" type="image/x-icon">
    <link href="/css/style.css" rel="stylesheet">
    <link href="/css/vehicles.css" rel="stylesheet">
</head>

<body>

    <!-- Page Title -->
    <div class="page-header-minimal">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <h1 class="page-title-minimal">{{bodyType}} Cars</h1>
                    <p class="page-subtitle-minimal">{{totalCount}} vehicles available</p>
                </div>
                <div class="col-md-4 text-right">
                    <div class="vehicle-count-badge">
                        <i class="fas fa-car"></i>
                        <span>{{totalCount}} Cars</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Filter Section -->
    <section class="py-4 bg-light">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12">
                    <div class="filter-card">
                        <div class="filter-header">
                            <h5><i class="fas fa-filter"></i> Filter Cars</h5>
                        </div>
                        <form method="GET" action="/vehicles/{{bodyType}}/filter" class="filter-form">
                            <div class="row g-3">
                                <div class="col-lg-3 col-md-6">
                                    <div class="form-group">
                                        <label for="brand"><i class="fas fa-car"></i> Brand</label>
                                        <select class="form-control" id="brand" name="brand">
                                            <option value="">All Brands</option>
                                            <option value="BMW">BMW</option>
                                            <option value="Audi">Audi</option>
                                            <option value="Mercedes-Benz">Mercedes-Benz</option>
                                            <option value="Ford">Ford</option>
                                            <option value="Volkswagen">Volkswagen</option>
                                            <option value="Toyota">Toyota</option>
                                            <option value="Honda">Honda</option>
                                            <option value="Nissan">Nissan</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="col-lg-2 col-md-6">
                                    <div class="form-group">
                                        <label for="minPrice"><i class="fas fa-pound-sign"></i> Min Price</label>
                                        <input type="number" class="form-control" id="minPrice" name="minPrice" placeholder="£0">
                                    </div>
                                </div>
                                <div class="col-lg-2 col-md-6">
                                    <div class="form-group">
                                        <label for="maxPrice"><i class="fas fa-pound-sign"></i> Max Price</label>
                                        <input type="number" class="form-control" id="maxPrice" name="maxPrice" placeholder="£100k">
                                    </div>
                                </div>
                                <div class="col-lg-2 col-md-6">
                                    <div class="form-group">
                                        <label for="transmission"><i class="fas fa-cog"></i> Transmission</label>
                                        <select class="form-control" id="transmission" name="transmission">
                                            <option value="">All</option>
                                            <option value="Manual">Manual</option>
                                            <option value="Automatic">Automatic</option>
                                            <option value="Semi-Automatic">Semi-Auto</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="col-lg-2 col-md-6">
                                    <div class="form-group">
                                        <label for="fuelType"><i class="fas fa-gas-pump"></i> Fuel</label>
                                        <select class="form-control" id="fuelType" name="fuelType">
                                            <option value="">All</option>
                                            <option value="Petrol">Petrol</option>
                                            <option value="Diesel">Diesel</option>
                                            <option value="Electric">Electric</option>
                                            <option value="Hybrid">Hybrid</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="col-lg-1 col-md-6">
                                    <div class="form-group">
                                        <label>&nbsp;</label>
                                        <button type="submit" class="btn btn-primary w-100">
                                            <i class="fas fa-search"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Vehicles Grid -->
    <section class="py-5">
        <div class="container">
            <div id="vehicles-container">
                {{#if vehicles.length}}
                    <div class="row" id="vehicles-grid">
                        {{#each vehicles}}
                        <div class="col-lg-4 col-md-6 mb-4">
                            <div class="card h-100 vehicle-card" onclick="window.location.href='/vehicles/{{bodyType}}/details/{{_id}}'" style="cursor: pointer;">
                                <div class="vehicle-image-container">
                                    <img class="card-img-top" src="{{imagePath}}?v={{@index}}&t={{timestamp}}" alt="{{title}}" 
                                         onerror="this.src='/images/default-car.jpg'">
                                    <div class="vehicle-price">£{{price}}</div>
                                </div>
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title">{{title}}</h5>
                                    <p class="card-text text-muted">{{brand}} {{model}} • {{year}}</p>
                                    <div class="vehicle-specs">
                                        <small class="text-muted">
                                            <i class="fas fa-tachometer-alt"></i> {{mileage}} miles<br>
                                            <i class="fas fa-cog"></i> {{transmission}}<br>
                                            <i class="fas fa-gas-pump"></i> {{fuelType}}
                                        </small>
                                    </div>
                                    <div class="mt-auto">
                                        <div class="row">
                                            <div class="col-6">
                                                <a href="/vehicles/{{bodyType}}/details/{{_id}}" class="btn btn-outline-primary btn-sm w-100" onclick="event.stopPropagation();">View Details</a>
                                            </div>
                                            <div class="col-6">
                                                <a href="/vehicles/{{bodyType}}/book/{{_id}}" class="btn btn-primary btn-sm w-100" onclick="event.stopPropagation();">Book Now</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        {{/each}}
                    </div>
                {{else}}
                    <div class="text-center py-5">
                        <h3>No {{bodyType}} cars found</h3>
                        <p class="text-muted">Try adjusting your filters or check back later for new listings.</p>
                        <a href="/cars" class="btn btn-primary">View All Cars</a>
                    </div>
                {{/if}}
            </div>
            
            <!-- Load More Button -->
            {{#if hasMore}}
            <div class="text-center mt-4">
                <button id="load-more-btn" class="btn btn-outline-primary btn-lg">
                    <i class="fas fa-plus"></i> Load More Cars
                </button>
            </div>
            {{/if}}
            
            <!-- Loading Spinner -->
            <div id="loading-spinner" class="text-center mt-4" style="display: none;">
                <div class="spinner-border text-primary" role="status">
                    <span class="sr-only">Loading...</span>
                </div>
                <p class="mt-2">Loading more cars...</p>
            </div>
        </div>
    </section>


    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <span class="copyright">Copyright &copy; Merryfield Trading Ltd {{currentYear}}</span>
                </div>
                <div class="col-md-4">
                    <ul class="list-inline social-buttons">
                        <li class="list-inline-item">
                            <a href="#"><i class="fab fa-twitter"></i></a>
                        </li>
                        <li class="list-inline-item">
                            <a href="#"><i class="fab fa-facebook-f"></i></a>
                        </li>
                        <li class="list-inline-item">
                            <a href="#"><i class="fab fa-linkedin-in"></i></a>
                        </li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <ul class="list-inline quicklinks">
                        <li class="list-inline-item"><a href="#">Privacy Policy</a></li>
                        <li class="list-inline-item"><a href="#">Terms of Use</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.bundle.min.js"></script>
    <script>
        $(document).ready(function() {
            let currentPage = {{currentPage}} || 1;
            let hasMore = {{hasMore}} || false;
            let isLoading = false;
            const bodyType = '{{bodyType}}' || 'All';
            
            // Load more vehicles function
            function loadMoreVehicles() {
                if (isLoading || !hasMore) return;
                
                isLoading = true;
                currentPage++;
                
                // Show loading spinner
                $('#loading-spinner').show();
                $('#load-more-btn').hide();
                
                // Make AJAX request
                $.get('/vehicles', {
                    bodyType: bodyType === 'All' ? '' : bodyType,
                    page: currentPage,
                    limit: 20,
                    format: 'json'
                })
                .done(function(data) {
                    if (data.vehicles && data.vehicles.length > 0) {
                        // Append new vehicles to grid
                        const vehiclesGrid = $('#vehicles-grid');
                        data.vehicles.forEach(function(vehicle) {
                            const vehicleCard = createVehicleCard(vehicle);
                            vehiclesGrid.append(vehicleCard);
                        });
                        
                        // Update hasMore flag
                        hasMore = data.hasMore;
                        
                        // Show/hide load more button
                        if (hasMore) {
                            $('#load-more-btn').show();
                        }
                    } else {
                        hasMore = false;
                    }
                })
                .fail(function() {
                    console.error('Failed to load more vehicles');
                    currentPage--; // Revert page number on error
                })
                .always(function() {
                    isLoading = false;
                    $('#loading-spinner').hide();
                });
            }
            
            // Create vehicle card HTML
            function createVehicleCard(vehicle) {
                return `
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="card h-100 vehicle-card" onclick="window.location.href='/vehicles/${vehicle.bodyType}/details/${vehicle._id}'" style="cursor: pointer;">
                            <div class="vehicle-image-container">
                                <img class="card-img-top" src="${vehicle.imagePath}?v=${Date.now()}" alt="${vehicle.title}" 
                                     onerror="this.src='/images/default-car.jpg'">
                                <div class="vehicle-price">£${vehicle.price}</div>
                            </div>
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title">${vehicle.title}</h5>
                                <p class="card-text text-muted">${vehicle.brand} ${vehicle.model} • ${vehicle.year}</p>
                                <div class="vehicle-specs">
                                    <small class="text-muted">
                                        <i class="fas fa-tachometer-alt"></i> ${vehicle.mileage} miles<br>
                                        <i class="fas fa-cog"></i> ${vehicle.transmission}<br>
                                        <i class="fas fa-gas-pump"></i> ${vehicle.fuelType}
                                    </small>
                                </div>
                                <div class="mt-auto">
                                    <div class="row">
                                        <div class="col-6">
                                            <a href="/vehicles/${vehicle.bodyType}/details/${vehicle._id}" class="btn btn-outline-primary btn-sm w-100" onclick="event.stopPropagation();">View Details</a>
                                        </div>
                                        <div class="col-6">
                                            <a href="/vehicles/${vehicle.bodyType}/book/${vehicle._id}" class="btn btn-primary btn-sm w-100" onclick="event.stopPropagation();">Book Now</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
            }
            
            // Load more button click handler
            $('#load-more-btn').click(function() {
                loadMoreVehicles();
            });
            
            // Infinite scroll (optional - uncomment to enable)
            /*
            $(window).scroll(function() {
                if ($(window).scrollTop() + $(window).height() >= $(document).height() - 1000) {
                    loadMoreVehicles();
                }
            });
            */
            
        });
    </script>
</body>
</html>
